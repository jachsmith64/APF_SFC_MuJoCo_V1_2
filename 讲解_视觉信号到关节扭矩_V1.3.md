# 从「视觉测到的位移」到「关节扭矩」——V1.3 信息流讲解

> 给自己和组会用的一份小抄。每一步都写成 **「文字说明 + 公式」**，公式后面跟着它在代码里的位置和真实量级。
> 所有数字都来自本工程实测（P05R01 数据包；正式参数 `control_hz = 1000`、`physics_hz = 1000`，
> 见 §5 末尾关于 132 Hz 的说明）。

---

## 0. 一句话总览

**摄像头只给我们一个数：末端在 Y 方向上偏了多少（单位 µm）。这个数经过 6 步传递，最后变成 6 个关节上的力矩。**

```
e_y(位移误差)
  → a_y_est(加速度估计)      ← 对 e_y 的历史做因果二次拟合，二次项系数×2
  → F_vir(虚拟力)            ← 乘一个"等效质量"，正号
  → SFC 核心 ODE             ← 剪切增稠流体模型，输出一个速度
  → y_sfc_offset(位置偏移)   ← 把速度积回位移，接到已有位置伺服上
  → q_cmd(关节角指令)        ← DLS 逆运动学
  → τ(关节扭矩)              ← MuJoCo 位置伺服内部产生
```

一句话记住这条链的性质：**它不是"直接力控"，而是"用视觉算出一个很小的位置修正量，塞进原本就在跑的位置伺服里"。**

---

## 1. 第 0 步：先看清有两个完全不同的「力」

讲的时候最容易讲混的就是这里，务必先说清楚。

| | `w(t)` | `F_vir` |
|---|---|---|
| 是什么 | **真实物理扰动** | **控制器自己算出来的虚拟力** |
| 从哪来 | `w_force.csv`（预实验反演，冻结文件） | 视觉测得的 `e_y` 算出来的 |
| 怎么施加 | MuJoCo `xfrc_applied` 加在 `wrist_3_link` | 只存在于控制器内部，是 SFC 方程的输入 |
| 谁看得见 | 被控对象（机械臂） | 控制器（`ctrl.step()`） |

代码上的铁律（`run.py`）：

```python
a_y_est, F_vir, mapper_ready = mapper.step(t, e_y)   # 控制器只看 e_y
v_sfc_out = ctrl.step(dt_actual, F_vir)              # 从不把 w.w(t) 传进来
```

也就是说：**控制器永远不知道真实的扰动力是多少**，它只能"看"到视觉测出来的位移。这正是我们想验证的——用视觉等价测量能不能替代直接测力。公平 A/B 里 `w(t)` 对两组逐字节相同，唯一的差别是有没有 SFC 这一环。

---

## 2. 第 1 步：视觉信号 → 位移误差 `e_y`

摄像头（或仿真里等价的位置测量）给出末端实际位置，减去名义路径给出的期望位置，就是误差。

$$
e_y(t) = y_{act}(t) - y_{ref}(t)
$$

- `y_ref` 只沿 X 走（回放 `schedule.csv`），Y 方向名义上应该**一直待在原地**，所以 Y 方向的偏差全是"被扰动带出来的振"。
- 代码：`run.py` 每个控制节拍算一次，同时写进 `trajectory.csv` 的 `e_y` 列。
- 量级（冻结 `e_des` 实测）：时域 RMS **28.62 µm**、峰值 **125.4 µm**；按冻结频带拆开——
  慢成分（0–2.5 Hz）RMS **16.46 µm**，主振动簇（**2.50–14.01 Hz**，主导峰 3.50 Hz）RMS **10.85 µm**。

**为什么要先取误差而不是直接用位置？** 因为我们要抑制的是"偏离期望"的那部分；路径本身怎么动，是另一回事。

---

## 3. 第 2 步：`e_y` 历史 → 加速度估计 `a_y_est`（因果二次拟合）

这一步是 V1.3 相对旧版最大的改动。旧版是"误差 × 比例增益"直接当力；**V1.3 先用一小段历史把加速度估出来**。

做法：取最近 `N` 个（本包 `N = 9`）真实的 (时间, 位移) 点，把它们**以"当前时刻"为原点**重新标号：

$$
\tau_i = t_i - t_{current}
$$

再用最小二乘拟合一条抛物线：

$$
e_y(\tau) = c_0 + c_1\,\tau + c_2\,\tau^2
$$

抛物线的一次项系数是速度、二次项系数是加速度的一半，所以：

$$
\boxed{\;a_{y\_est} = 2\,c_2\;}
$$

- **"因果"是什么意思**：只用当前时刻**之前**的点，绝不偷看未来。所以它是真能上机器人的在线算法，不是离线数据后处理。
- **为什么用二次拟合而不是差分**：直接用相邻两拍做二阶差分，噪声会被放大得不成样子（信号本身只有 µm 级）。在一段窗口上做最小二乘，等于顺带做了平滑。
- **窗口长度为什么是 9**：窗口越短，二阶项越"尖"，估出来的加速度越毛躁；整定时窗口取 5/7 会算出超出论文有效范围（1<n≤5）的 n，取 9 才落在范围内。见 §8。
- **保护逻辑（重要）**：刚开机、历史点不够 `N` 个时，不能瞎猜。代码里返回

$$
\text{mapper\_ready} = \text{False} \;\Rightarrow\; a_{y\_est} = 0 \;\Rightarrow\; F_{vir} = 0
$$

  也就是说**前几拍 SFC 一定是零输出**，这是设计如此，不是 bug。`trajectory.csv` 里专门记了 `mapper_ready` 这一列（1/0）。

- 代码：`apf_sfc.py` 的 `CausalAccelForceMapper.step()`。
- 量级：`a_y_est` 的 RMS 约 **0.129 m/s²**，峰值约 **0.838 m/s²**。

---

## 4. 第 3 步：加速度 → 虚拟力 `F_vir`

拿到加速度，乘一个"等效质量"就得到力。这是牛顿第二定律最朴素的一次应用：

$$
F_{vir} = +\,\texttt{force\_map\_mass\_kg}\;\cdot\; a_{y\_est}
$$

本包 `force_map_mass_kg = 1.0 kg`，所以数值上加速度是多少，虚拟力就是多少（N ↔ m/s²）。

**这个正号是硬规定，不能改。** 物理直觉很简单：

- 末端往 +Y 方向被甩出去（正向加速度）→ 虚拟力也是 +Y 方向 → 后续环节往反方向顶回去；
- **禁止**写成 `−force_map_mass_kg · a_y_est`（那等于把符号在入口处先翻一次，后面全乱）；
- SFC 输出端也**不加**人为的负号——因为"顶回去"这件事已经由方程内部的阻力项自然完成了（见下一节）。

顺便提醒一个容易混的点：`force_map_mass_kg`（这里是 1.0 kg，只是"把加速度换算成力"的标定系数）和 SFC 内部那个虚拟质量 `sfc_m`（= 1.0 kg，是 ODE 里的惯性项）**是两个不同的变量**，只是本包恰好都取了 1.0。

- 代码：`apf_sfc.py`，`F_vir = force_map_mass_kg * a_y_est`。
- 量级：整定统计给出 `|F_vir|` 的 P50 = 0.061 N、P99 = 0.404 N、最大 0.838 N。**注意这是"牛顿"量级的力，不是几十牛顿。**

---

## 5. 第 4 步：SFC 核心——剪切增稠虚拟 ODE

这是整条链的"心脏"，也是唯一一个真正的动力学方程。它模拟的是一种**剪切增稠流体**：搅得慢的时候很软，搅得快的时候一下子变硬。

$$
m\,\dot{v}_s + \mu\,|v_s|^{\,n-1}\,v_s = F_{vir}
$$

逐项读：

| 项 | 含义 |
|---|---|
| $m\,\dot{v}_s$ | 惯性项，虚拟质量 |
| $\mu\,|v_s|^{n-1}v_s$ | **阻力项**，$n>1$ 就是剪切增稠（速度越大阻力涨得越快） |
| $F_{vir}$ | 上一节算出来的虚拟力，唯一的输入 |

代码里把它拆成"先算阻力、再算加速度、再积分"三步（欧拉法）：

$$
\text{shear} = \mu\,\mathrm{sign}(v_s)\,|v_s|^{\,n}, \qquad
a_s = \frac{F_{vir} - \text{shear}}{m}
$$

$$
v_s \leftarrow v_s + a_s \cdot \Delta t_{actual}, \qquad
\boxed{\;v_{sfc\_out} = g \cdot v_s\;}
$$

**几个关键点：**

- **`dt_actual` 是真时间**。代码记录并使用的是**每拍真实间隔**，不是写死的 `1/control_hz`。物理步固定 1 ms；控制节拍若设成与物理同频（本包 `control_hz = 1000`），`dt_actual` 就是 1 ms；若设成 132 Hz，则物理步每 7.576 步触发一次，实际表现为 **7 ms / 8 ms 交替**。
- **`g` 是"速度到输出"的标定增益**（本包 `g = 0.01561887`）。SFC 核心对外**只输出一个速度** `v_sfc_out`（m/s），到此为止。
- **这里没有人为负号**：注意 `shear` 项本身就带着 $-\mu|v_s|^{n-1}v_s$ 的对抗方向，这就是"抑制"的来源。
- **离散稳定性有硬上界**。显式欧拉不能乱来，代码启动时先算一个上界：

$$
v_{ss} = \left(\frac{f_{max}}{\mu}\right)^{1/n}, \qquad
a_{lin} = \frac{n\,\mu\,|v_{ss}|^{\,n-1}}{m}, \qquad
\Delta t_{max} = \frac{2}{a_{lin}}
$$

  运行中一旦 `dt_actual` 超过它，`run.py` 会**直接抛错中止**——这是保护，不是可选检查。
- 代码：`apf_sfc.py` 的 `ApfSfc.step()` 和 `sfc_dt_max()`。
- 本包参数：$m=1.0$ kg，$\mu = 2.5336\times10^{8}$，$n = 4.6576$，$g = 0.01561887$。

### ⚠️ 关于 132 Hz（组会上可能被问到，务必说准）

本包整定后 $\Delta t_{max} = 0.0077432$ s，已经**小于 8 ms**。而 132 Hz 的真实控制间隔是
7 ms / 8 ms 交替（因为 1000/132 = 7.576 不是整数，见上一条），**那 8 ms 的拍子会超界**。
实测直接报错中止：

```
RuntimeError: [run] dt_actual=0.008000s 超过论文离散上界 dt_max≈0.0077s，中止。
```

所以**这份数据包当前只能用 `control_hz = 1000`（`dt_actual = 1 ms`）跑正式 A/B**。
想恢复 132 Hz，要么重新整定让 $\mu$ 或 $n$ 变小，要么改物理/控制频率比。

> 顺带纠一处口径：README 与 `tests/acceptance.py` 里的判据是
> `dt_max_s > 1/132 ≈ 7.576 ms`，拿的是**名义**周期；实际 132 Hz 存在 8 ms 的拍子，
> 所以这条判据通过**不等于** 132 Hz 可跑。

---

## 6. 第 5 步：SFC 的速度 → 位置偏移 `y_sfc_offset`（接口适配层）

**这一步必须讲清楚它的身份**：它**不属于 SFC 核心**，它是"把 SFC 的输出接到本工程已有的位置伺服上"的**适配层**。

$$
y_{sfc\_offset} \leftarrow y_{sfc\_offset} + v_{sfc\_out}\cdot \Delta t_{actual}
$$

$$
y_{cmd} = y_{ref} + y_{sfc\_offset}
$$

- 名字里的 `offset` 说得很直白：它**不替换**名义路径，只在名义路径上**叠加一个偏移**。SFC 输出为零的那些拍（未 ready、或抑制量本来就是 0），`offset` 就停在原地不动。
- A 组（基线）里这一项**恒等于 0**，`trajectory.csv` 的 `y_sfc_offset` 列全程为 0——这就是"唯一差别是有没有 SFC"在数据上的体现。
- 量级：实测 3 s 回放（`control_hz = 1000`）中 `y_sfc_offset` 峰值 **17.00 µm**，A 组全程 **0 µm**；
  同一拍 `e_y = 15.04 µm`、`y_cmd − y_ref = 17.00 µm`。也就是说，**SFC 到最后只挪动了十几微米**。

---

## 7. 第 6 步：位置指令 → 关节角 → 关节扭矩

### 7.1 目标位姿

$$
p_{cmd} = \big[\,x_{ref}(t),\;\; y_{cmd},\;\; z_{ref} \,\big], \qquad R_{cmd} = R_0
$$

姿态全程锁死成初始姿态（这一版只做平移，不转 TCP）。

### 7.2 DLS 逆运动学（阻尼最小二乘）

从**实测**关节角出发，反复迭代求一个让末端到达目标的关节角：

$$
\Delta q = J^{\mathsf T}\left(JJ^{\mathsf T} + \lambda^2 I\right)^{-1} e,
\qquad q_{cmd} = q_{meas} + \Delta q
$$

其中 $e = [\,e_p;\; e_o\,]$ 是位置误差和姿态误差（姿态误差用旋转矢量表示），$J$ 是 6×nv 的雅可比，$\lambda = 10^{-4}$ 是阻尼系数。

- **阻尼加在 `JJᵀ + λ²I` 上**（这是正确的一侧）：关节接近奇异时 $JJ^{\mathsf T}$ 病态，加阻尼能防止 $\Delta q$ 爆掉。若误写成 $(J^{\mathsf T}J+\lambda^2I)^{-1}J^{\mathsf T}$ 那一侧就会失效。
- 每个控制节拍最多迭代 3 轮，然后**把仿真状态还原成实测值**（IK 只是"解算"，不推进物理）。
- 代码：`kinematics.py` 的 `dls_ik()`。

### 7.3 位置伺服 → 扭矩（扭矩到底从哪来）

**这里是最关键的一句：本工程不做直接力控，关节扭矩不是我们算出来的，是 MuJoCo 位置伺服内部产生的。**

`assets_ur10e/ur10e.xml` 里的 actuator 定义是：

```xml
<general biastype="affine" gainprm="5000" biasprm="0 -5000 -500"/>
```

它的意思是（本工程加载即用，**不改 XML**）：

$$
\tau = k_p\,(q_{cmd} - q) - k_v\,\dot{q},
\qquad k_p = 5000,\quad k_v = 500
$$

读作：**"你希望关节在 `q_cmd`，实际在 `q`，差多少我就按 5000 的比例使劲扳过去，同时用 500 的阻尼压住转速。"** 力矩上下限是 ±330 N·m（大关节）/ ±150 N·m（小关节）。

所以完整的一句话是：

> **视觉 → 加速度 → 虚拟力 → SFC 速度 → 十几微米的位置偏移 → 逆解成关节角指令 → 位置伺服按 `5000·(q_cmd−q)` 把它变成关节扭矩。**

**绝不能表述成"SFC 直接输出关节扭矩"**——SFC 只输出一个速度，中间隔着一个完整的位置伺服。

---

## 8. 把真实数字串起来（组会上举这个例子最直观）

同一物理初值、同一 `w(t)`、同一 IK，只切换有没有 SFC，3 s 回放（`control_hz = physics_hz = 1000`）实测：

```
== SFC 链对关节扭矩的净贡献（B − A）==
  物理步数 3000   关节数 6
  max|Δτ|  = 0.001099 N·m  @ t=1.835 s  关节#0
  rms|Δτ|  = 0.000176 N·m
  名义扭矩 max|τ_A| = 40.50 N·m      （纯跟踪 + 重力，与 SFC 无关）
  y_sfc_offset 峰值 = 17.00 µm（A 组恒为 0）
  a_y_est 峰值 = 0.2257 m/s²   F_vir 峰值 = 0.2257 N
  sfc_v_out 峰值 = 2.576e-05 m/s
  A/B 的 F_vir 逐拍完全相同 = True   （控制器只吃 e_y，与 w(t) 无关）
```

**怎么读这组数：**

1. 位移出口只有 **17 µm**。按 TCP 到关节的量级折算，逆解出的关节角修正约 **10⁻⁵ rad** 级别。
2. 最终净扭矩 **峰值 0.0011 N·m**，而同时段名义扭矩峰值是 **40.5 N·m**——比值约 **1/37000**。
3. 换句话说：**这条链在这台刚性臂上的"执行权限"极小**。这不是算错了，而是三个环节的量级层层缩小共同造成的：视觉误差本来就只有几十 µm → 虚拟力只有零点几 N → SFC 因为 $\mu$ 极大（$2.5\times10^{8}$）而几乎不产生速度 → 偏移只有十几 µm。
4. 这正好解释了 README 里记录的边界：**在这台用强关节伺服的刚性 UR10e 上，SFC 无法对 3.5 Hz 本征振动做有效功**（因为振动是关节/连杆刚度决定的，不是"末端被推着走"），要验证抑制效果需要更柔性的 plant。这是如实记录的结论，不能推广成"SFC 无效"。

---

## 9. 组会 60 秒口播稿（照着念就行）

> 摄像头测到的其实只有一个量：末端在 Y 方向偏了多少，几十微米。
> 我不直接拿这个偏差去乘增益，而是先用最近 9 个采样点做一次因果的二次拟合，抛物线的二次项系数乘 2 就是加速度——因果的意思是只用过去的数据，不偷看未来。
> 加速度乘一个等效质量得到虚拟力，只有零点几牛顿，注意这里必须是正号。
> 虚拟力送进一个剪切增稠流体的方程，阻力项是 μ 乘速度的 n 次方，n 大约 4.66，这就是"搅得越快越硬"；这个方程积分出来的速度乘上增益，就是 SFC 的全部输出。
> 这个速度再积分成十几微米的位置偏移，叠加到名义路径上，交给逆运动学解出关节角指令。
> 最后真正出力矩的是 MuJoCo 自带的位置伺服，扭矩等于 5000 乘上"期望角减实际角"，再减 500 乘角速度。所以扭矩不是我直接给的，是位置伺服产生的。
> 实测下来 SFC 这一环对扭矩的净贡献峰值是 0.0011 牛米，而同时段的名义扭矩峰值是 40.5 牛米，差了大约四个半数量级——这也解释了为什么在这台刚性臂上看不到明显抑制。

---

## 10. 可能被问到的几个问题

**Q：为什么不用误差直接乘增益？非要估加速度？**
A：直接乘增益是"弹簧"，本质是位置反馈；这一版想验证的是**基于视觉等效测量的力估计**能不能替代力传感器。加速度×质量才是"力"的量纲，物理上更站得住。

**Q：窗口为什么取 9？不能取 5 更灵敏吗？**
A：能取，但整定结果会超范围。实测同一段 `e_des`：窗口 5 算出 n = 11.23，窗口 7 算出 5.97，都超出论文公式有效范围 1<n≤5；窗口 9 才得到 4.6576。因为窗口越短，二阶差分越尖，P99/P50 的比值越大，n 就越大。`sfc_tune.py` 遇到这种情况会直接报错并提示调 `velocity_ratio`，不会静默通过。

**Q：`mapper_ready` 是 0 的那几拍怎么办？**
A：输出严格为零（加速度 0 → 虚拟力 0 → SFC 输入为 0）。开机头几拍本来就没有历史点，宁可不动作也不能瞎估。

**Q：dt_actual 为什么不用 1/control_hz 直接当常数？**
A：因为真实间隔并不均匀。物理步固定 1 ms，控制周期与物理步不是整数倍关系时（比如 132 Hz），实际表现为 7 ms / 8 ms 交替。用真实间隔积分才对，CSV 里也如实记了这一列。而且有离散稳定上界 Δt_max = 7.74 ms 卡着——**这一版的 132 Hz 正好被这个上界挡住**（8 ms 的拍子超界），详见 §5 末尾。

**Q：为什么这份数据包是 1000 Hz 控制，不是 132 Hz？**
A：因为整定出的 μ 很大（2.53×10⁸）、n = 4.66，离散稳定上界算下来只有 7.74 ms，132 Hz 的 8 ms 拍子会超界并被 `run.py` 明确拒绝。1000 Hz 下 dt_actual = 1 ms，远在上界之内。这条限制是整定结果的直接推论，如实记录。

**Q：A/B 公平性怎么保证？**
A：同一份 params 快照、同一 `INIT_Q`、同一份逐字节相同的 `w(t)` 和 `schedule.csv`，唯一差别是 B 组实例化了 `ApfSfc`。而且 `ctrl.step()` 只接收 `F_vir`，控制器代码里**没有任何路径**能读到 `w_force`——这点在 `run.py` 里是显式写死的。

---

## 11. 讲的时候千万别讲错的三句话

1. ❌「SFC 直接输出关节扭矩」 → ✅「SFC 输出一个速度，经位置偏移和逆解后，由 MuJoCo 位置伺服产生扭矩」
2. ❌「控制器根据扰动大小算出力」 → ✅「控制器只看到视觉测出的位移误差，从不知道真实扰动力 `w(t)` 是多少」
3. ❌「虚拟力是 −m·a」 → ✅「`F_vir = +force_map_mass_kg · a_y_est`，正号；抑制方向由 SFC 方程内部的阻力项提供」

---

## 12. 代码对照表（被追问时快速定位）

| 环节 | 公式 | 文件 | 函数 / 位置 |
|---|---|---|---|
| 位移误差 | $e_y = y_{act}-y_{ref}$ | `run.py` | 控制节拍主循环 |
| 加速度估计 | $a_{y\_est}=2c_2$，$\tau_i=t_i-t_{now}$ | `apf_sfc.py` | `CausalAccelForceMapper.step()` |
| 虚拟力 | $F_{vir}=+m_{map}a_{y\_est}$ | `apf_sfc.py` | 同上 |
| SFC ODE | $m\dot v_s+\mu|v_s|^{n-1}v_s=F_{vir}$ | `apf_sfc.py` | `ApfSfc.step()` |
| 离散上界 | $\Delta t_{max}=2/a_{lin}$ | `apf_sfc.py` | `sfc_dt_max()` |
| 位置偏移 | $y_{cmd}=y_{ref}+\int v_{sfc\_out}dt$ | `run.py` | 运行层积分 |
| 逆解 | $\Delta q=J^{\mathsf T}(JJ^{\mathsf T}+\lambda^2I)^{-1}e$ | `kinematics.py` | `dls_ik()` |
| 关节扭矩 | $\tau=5000(q_{cmd}-q)-500\dot q$ | `assets_ur10e/ur10e.xml` | `<general gainprm/biasprm>` |
| 整定 | 论文 Algorithm 1 | `sfc_tune.py` | `tuning_from_e_des()` |

---

## 附：本包（P05R01）实际参数一览

| 参数 | 值 | 含义 |
|---|---|---|
| `e_y` RMS（冻结 `e_des`） | 28.62 µm（峰值 125.4） | 视觉位移误差；主振动簇 2.50–14.01 Hz 占 10.85 µm |
| `a_y_est` RMS / 峰值 | 0.129 / 0.838 m/s² | 因果二次拟合的输出 |
| `force_map_mass_kg` | 1.0 kg | 加速度 → 力的换算系数 |
| `accel_window_points` | 9 | 拟合窗口点数（奇数，≥3） |
| `F_vir` P50 / P99 / max | 0.06110 / 0.40381 / 0.83782 N | 虚拟力统计（整定用） |
| `sfc_m` | 1.0 kg | SFC 惯性项（≠ `force_map_mass_kg`） |
| `sfc_mu` | 2.533582×10⁸ | 剪切增稠系数 |
| `sfc_n` | 4.6576149 | 剪切增稠指数（1<n≤5） |
| `sfc_g` | 0.01561887 | SFC 速度 → 输出速度的增益 |
| `dt_max_s` | 0.0077432 s | 离散稳定上界（< 8 ms ⇒ 本包 132 Hz 不可跑，见 §5） |
| 实测 `y_sfc_offset` 峰值 | 17.00 µm（3 s，1000 Hz） | SFC 链在位移上的总出口（A 组 0） |
| 实测 `Δτ` 峰值 / RMS | 0.001099 / 0.000176 N·m | SFC 链对关节扭矩的净贡献 |
| 名义 `τ` 峰值 | 40.50 N·m | 跟踪 + 重力，与 SFC 无关 |
| 校验跑（3 s，A/B 各一次） | 参考量 | `a_y_est` 峰 0.2257 m/s²、`F_vir` 峰 0.2257 N、`sfc_v_out` 峰 2.576e-05 m/s |
